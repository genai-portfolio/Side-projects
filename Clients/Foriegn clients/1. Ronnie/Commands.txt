Use this command to install libraries

pip3 install -r requirements.txt

Once, the libraries are installed, try running the file with the command:

python3 launcher.py




Then we'll make it an app, write this line of code:

pyinstaller --noconsole --onefile --name "LeadsCampus Scraper" launcher.py